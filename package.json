{
  "name": "drone-racing",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "start": "turbo run start --parallel",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "workspaces": [
    "api",
    "web"
  ],
  "packageManager": "npm@11.5.2",
  "devDependencies": {
    "turbo": "^2.5.6"
  }
}